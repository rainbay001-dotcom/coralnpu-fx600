`ifndef SYNTHESIS // Added by Core.scala Verification Wrapper

// Generated by CIRCT firtool-1.114.0-2-g6cf2492e1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module FetchControl_Verification_Assert(
  input        predecode_firstJumpOH_2,
               predecode_firstJumpOH_3,
               predecode_firstJumpOH_0,
               predecode_firstJumpOH_1,
               reset,
               _GEN,
               ongoingFetch_valid,
               _GEN_0,
  input [31:0] io_fetchAddr_out_bits,
               io_fetchAddr_prevBits,
  input        clock,
               io_fetchAddr_prevPending,
               io_fetchAddr_out_valid
);

  `ifndef SYNTHESIS
    always @(posedge clock) begin
      if (~reset
          & {1'h0, {1'h0, predecode_firstJumpOH_0} + {1'h0, predecode_firstJumpOH_1}}
          + {1'h0,
             {1'h0, predecode_firstJumpOH_2}
               + {1'h0, predecode_firstJumpOH_3}} > {2'h0, _GEN}) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at Library.scala:235\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & {1'h0, ongoingFetch_valid} + {1'h0, _GEN_0} > {1'h0, _GEN}) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at Library.scala:235\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (io_fetchAddr_prevPending & ~reset & ~io_fetchAddr_out_valid) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: IrrevocableIO violation: valid dropped\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (io_fetchAddr_prevPending & ~reset
          & io_fetchAddr_out_bits != io_fetchAddr_prevBits) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: IrrevocableIO violation: bits changed\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule


`endif // Added by Core.scala Verification Wrapper
