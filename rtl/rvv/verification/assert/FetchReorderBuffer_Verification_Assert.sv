`ifndef SYNTHESIS // Added by Core.scala Verification Wrapper

// Generated by CIRCT firtool-1.114.0-2-g6cf2492e1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module FetchReorderBuffer_Verification_Assert(
  input       reset,
  input [1:0] _GEN,
              _GEN_0,
              _GEN_1,
  input       _noDuplicateTxid_T_36,
              _noDuplicateTxid_T_35,
              _GEN_2,
              _freeTxidUsage_WIRE_1,
              _freeTxidUsage_WIRE_2,
              _freeTxidUsage_WIRE_0,
              _GEN_3,
              _GEN_4,
              _GEN_5,
              _GEN_6,
              clock,
              _GEN_7,
              _GEN_8,
              _GEN_9,
              _GEN_10,
              _GEN_11
);

  `ifndef SYNTHESIS
    wire [1:0] _GEN_12 = {1'h0, _GEN_3};
    always @(posedge clock) begin
      if (~reset & ~_GEN_7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at UncachedFetch.scala:264\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~_GEN_8) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at UncachedFetch.scala:278\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~_GEN_9) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at UncachedFetch.scala:283\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~_GEN_10) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at UncachedFetch.scala:290\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~_GEN_11) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at UncachedFetch.scala:298\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset
          & ~(_GEN_0 <= _GEN_1 & _GEN <= _GEN_0
              & ~(_noDuplicateTxid_T_36 | _noDuplicateTxid_T_35)
              & (_GEN == 2'h0 | _GEN_2))) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at UncachedFetch.scala:302\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & {1'h0, _freeTxidUsage_WIRE_0} + {1'h0, _freeTxidUsage_WIRE_1}
          + {1'h0, _freeTxidUsage_WIRE_2} > _GEN_12) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at UncachedFetch.scala:309\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & {1'h0, _GEN_6} + {1'h0, _GEN_4} + {1'h0, _GEN_5} > _GEN_12) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed at Library.scala:235\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule


`endif // Added by Core.scala Verification Wrapper
